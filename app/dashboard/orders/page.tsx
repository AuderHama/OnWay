export default function Home() {
  return <div> this will be the dashboard </div>;
  // create via API with customerName, phone, address, storeId, items[{productId, qty}].
  // pending → accepted → preparing → out_for_delivery → delivered, plus cancelled.
}
